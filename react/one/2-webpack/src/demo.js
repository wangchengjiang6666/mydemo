// react代码

// 1. 需要引入 React 与 ReactDOM
import React from "react";
import ReactDOM from "react-dom";

// 2. 调用 ReactDOM.render 方法，渲染出去一个hello react
ReactDOM.render(<h1>Hello React</h1>, document.getElementById("app"));
